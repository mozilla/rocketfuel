@import 'lib';

.loadmore {
    list-style: none;
    position: relative;
    padding: 15px;
    span {
        display: none;
    }
    button {
        display: block;
        margin: auto;
        max-width: 400px;
        padding: 0;
        width: 100%;
        &.hide, &:not(.hide) + .spinner {
            display: none;
        }
    }
    .throbber {
        display: none;
    }
    &.loading {
        button {
            visibility: hidden;
        }
        .throbber {
            animation(unquote('spin 1s linear infinite'));
            border: 4px solid #666;
            border-color: #666 transparent;
            border-radius: 20px;
            bottom: 0;
            display: block;
            height: 32px;
            left: 0;
            margin: auto;
            position: absolute;
            right: 0;
            top: 0;
            width: 32px;
        }
    }
    &.pagination-error span.error-text {
        display: block;
        text-align: center;
    }
}

.listing .loadmore {
    height: auto;
    min-height: 78px;
}
